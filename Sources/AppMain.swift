import SwiftUI

@main
struct ScrollViewApp: App {
    var body: some Scene {
        Window("ScrollView 滚动视图", id: "main") {
            ContentView()
        }
        .defaultSize(width: 400, height: 500)
    }
}
